# Capture Daemon Directory

This directory contains userspace capture tools for exfiltrating MT7925 testmode and ICAP binary frames.

- `src/`: C / Python capture daemon source code.
- `scripts/`: Helper scripts for setting up monitor interfaces and launching captures.
- `tests/`: Unit tests for capture ring buffers.
