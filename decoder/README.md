# Standalone MT7925 Payload Decoder

Zero-dependency Python/C decoder library for parsing raw MT7925 testmode response frames.

- `src/`: Core decoder library (`libruview_mt7925`).
- `fixtures/`: Sanitized test vectors for unit testing.
- `tests/`: Unit tests for subcarrier bit-unpacking and phase extraction.
